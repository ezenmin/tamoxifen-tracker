{
  "name": "tamoxifen-tracker",
  "version": "0.1.0",
  "description": "Track Tamoxifen side effects and share summaries with your doctor",
  "private": true,
  "scripts": {
    "dev": "npx http-server public -p 3000 -c-1",
    "test": "node tests/run-tests.js",
    "test:e2e": "npx playwright test",
    "test:lighthouse": "npx lhci autorun"
  },
  "devDependencies": {
    "@lhci/cli": "^0.14.0",
    "@playwright/test": "^1.49.0",
    "http-server": "^14.1.1",
    "supabase": "^2.72.7"
  }
}
